<%= form_with(model: [@shark, post]) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <%# <div>
    <%= form.label :shark_id, style: "display: block" %>
    <%# <%= form.text_field :shark_id %> %>
  <%# </div> %> %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
